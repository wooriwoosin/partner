# 프로젝트 규칙 (업체 통합 관리앱)

## 디자인 — 절대 규칙
- **무조건 라이트모드.** 다크모드 절대 금지. 새 화면/컴포넌트를 만들 때도 항상 밝은 배경(흰색/연회색) 기준으로 작성한다.
  - 기준 팔레트: 배경 `#f4f6f9`, 카드 `#ffffff`, 포인트 `#2563eb` (assets/styles.css `:root` 참고)
- UI 텍스트는 전부 한국어.

## 아키텍처
- 화면(HTML/JS/CSS)은 **깃허브 저장소에만** 둔다. Apps Script에는 HTML을 올리지 않는다 (API 전용).
- 메인 DB: 구글시트 `협력점 관리` (`1shhA5RdXP7DiaMIyR33bTG2jFj4SFqumYflY0lF0pwc`)의 `업체관리` 탭. 앞으로 이 DB만 사용.
- 기존 ②계산서방식/③전자계약서 시트는 참고용 소스일 뿐, 새 기능은 메인 DB만 읽고 쓴다.
- 신규 업체 등록·기호 현행화는 [개통리스트 업로드]로만 한다 (수동 등록 버튼 없음).
- Code.gs를 수정하면 사용자가 직접 붙여넣어야 하므로, 수정 시 파일을 채팅으로 전달하고 "배포 관리 → 새 버전" 안내를 함께 한다.

## 분류 규칙 요약
- 기호: `■□`=판매점, `★☆◆◇`=협력점(◆◇는 인센티브), 숫자접두/`●`=자점
- 자점은 계산서를 발행하지 않는다 (계산서 화면에서 제외)
