# 프로젝트 규칙 (업체 통합 관리앱)

## 디자인 — 절대 규칙
- **무조건 라이트모드.** 다크모드 절대 금지. 새 화면/컴포넌트를 만들 때도 항상 밝은 배경(흰색/연회색) 기준으로 작성한다.
  - 기준 팔레트: 배경 `#f4f6f9`, 카드 `#ffffff`, 포인트 `#2563eb` (assets/styles.css `:root` 참고)
- UI 텍스트는 전부 한국어.

## 아키텍처
- 화면(HTML/JS/CSS)은 **깃허브 저장소에만** 둔다. Apps Script에는 HTML을 올리지 않는다 (API 전용).
- 메인 DB: 구글시트 `협력점 관리` (`1shhA5RdXP7DiaMIyR33bTG2jFj4SFqumYflY0lF0pwc`)의 `업체관리` 탭. 앞으로 이 DB만 사용.
- 기존 ②계산서방식/③전자계약서 시트는 참고용 소스일 뿐, 새 기능은 메인 DB만 읽고 쓴다.
- 신규 업체 등록은 [개통리스트 업로드]로만 한다 (수동 등록 버튼 없음).
- **업로드 화면은 개통리스트 전용.** 소속 원장(전체사원관리) 등 다른 파일은 거부한다 — 실제 개통이 없는 업체가 신규 등록되거나 휴면이 풀리는 사고를 막기 위함.
- 기호·마커 일괄 변경은 시트의 `현행화` 탭 + `일괄현행화_미리보기()` → `일괄현행화_적용()` 으로만 한다 (신규 생성·상태 변경 없음).
- 업체 매칭은 괄호 부가설명 차이를 흡수해야 한다 (예: 시트 `기가몬스터(에이블정보통신)` ↔ 파일 `기가몬스터`). 매칭 실패로 인한 중복 등록은 치명적.
- Code.gs를 수정하면 사용자가 직접 붙여넣어야 하므로, 수정 시 파일을 채팅으로 전달하고 "배포 관리 → 새 버전" 안내를 함께 한다.

## 시트 데이터 규칙
- 시트 데이터는 **컬럼 이름 기준**으로 읽고 쓴다 (위치 기준 금지). 사용자가 시트에서 열 순서를 바꿔도 깨지지 않아야 한다.
- 컬럼을 추가할 때는 `HEADERS`에 넣고, 시트에는 **없는 컬럼만 뒤에 추가**한다. 기존 열을 자동으로 지우거나 재배치하지 않는다.
- 컬럼 구성을 바꾸면 `docs/시트-컬럼-안내.md`를 함께 갱신한다 (사용자가 시트 일괄 수정 시 보는 문서).
- 파괴적인 시트 재구성(`시트정리()`)은 반드시 백업 탭을 먼저 만들고, 사라지는 값은 새 컬럼으로 이전한 뒤 제거한다.

## 계정 권한
- **어드민**: 모든 기능 + 계정 추가·삭제 + 타인 비밀번호 초기화
- **스태프**: 업체 조회·등록·수정은 어드민과 동일, 본인 비밀번호 변경만 가능 (계정 관리 불가)
- 권한 판정은 항상 서버(사용자 시트)가 기준. 프론트의 숨김 처리는 보조 수단일 뿐이므로 서버에서도 반드시 검사한다.

## 변경로그
- 모든 데이터 변경은 `변경로그` 시트에 기록 (일시·아이디·이름·작업·대상·상세)
- 앱의 [📋 변경로그] 탭은 **이번 주(월요일~현재)** 기록만 조회 — 누적 로딩 방지
- 로그 기록 실패가 본 작업을 막지 않도록 항상 try/catch로 감싼다

## 분류 규칙 요약
- 기호: `■□`=판매점, `★☆◆◇`=협력점(◆◇는 인센티브), 숫자접두/`●`=자점
- **자점은 앱에서 아예 다루지 않는다** (목록·개통리스트 업로드 모두 제외)
- 상태: 활성 / 휴면(미거래). 휴면은 계산서·계약 화면에 표시하지 않는다
- 웹 관련 항목은 웹접수 Y/N · 웹회신 Y/N 두 개만 사용
  - 마커 **O = 우리가 대신 전달** → 웹접수 N · 웹회신 N
  - 마커 **X = 업체가 직접 웹처리** → 웹접수 Y · 웹회신 Y
  - 판매점은 대부분 마커 표기가 없으므로 **기본 O** (일부만 X)
- 개통리스트의 마커는 **`유치자` 열**의 (O)/(X) 표기에서 읽는다 (소속 원문에는 없음)
- 계약 필드: 위탁판매 / 개인정보 / 보증보험 (사업자등록증은 계산서 화면 소속)
